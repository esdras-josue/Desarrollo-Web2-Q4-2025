/**
 * 🧩 Ejercicio: “El Analizador de Números”

Enunciado:
Escribe una función que reciba un array de números y devuelva un objeto con esta información:

Cuántos números son positivos, negativos y ceros.

El número mayor y el menor.

El promedio de todos los números.

Una propiedad booleana isBalanced que sea true si el promedio está entre -1 y 1.
 */

const numbers: number[] = [2,4,8,0,-5,10,-9];

function numberAnalizer(): []{

    for(let i = 0; i < numbers.length; i++){
        
    }
    return [];
}
